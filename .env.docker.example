# Docker Environment Variables Example
# Copy this file to .env in the root directory when using docker-compose

# OpenAI API Key (replace with your actual key)
OPENAI_KEY=your-openai-api-key-here

# Note: Other environment variables are set in docker-compose.yml
# This file is only for sensitive keys that shouldn't be committed to git
